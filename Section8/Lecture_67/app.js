/*
  Semantic versioning (Semver) - version of code that conveys meaning.

  MAJOR.MINOR.PATCH

  PATCH is inscremented whenever bugs are fixed.
  MINOR is inscremented whenever features are added and the code is backwards compatible.
  MAJOR is incremented whenever the code has changed is not backwards compatible.
*/
